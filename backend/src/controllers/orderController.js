export const createOrder = async (req, res) => {
  // 1. Get checkout data
  // 2. Validate products
  // 3. Calculate prices
  // 4. Create Order
  // 5. Save to MongoDB
  // 6. Return created order
};